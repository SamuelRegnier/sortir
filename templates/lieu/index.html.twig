{% extends 'base.html.twig' %}

{% block title %}Hello LieuController!{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Ajouter">
    {{ form_end(form) }}
    <a href="{{ path('app_creer_sortie') }}"><button >Retour</button></a>
{% endblock %}

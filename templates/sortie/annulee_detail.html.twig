{% extends 'base.html.twig' %}

{% block body %}
    {% if sortie.etats.id == 6 %}
        <h1>La sortie {{ sortie.nom }} est annulée !</h1>
        <div class="affSortie">
            <div>
                <p>Nom de la sortie : </p>
                <p>Date de la sortie : </p>
                <p>Site organisateur : </p>
                <p>Lieu : </p>
                <label for="motif">Motif : </label>
            </div>
            <div>
                <p>{{ sortie.nom }}</p>
                <p>{{ sortie.dateHeureDebut|date('d-m-Y') }}</p>
                <p>{{ sortie.site.nom }}</p>
                <p>{{ sortie.lieux.nom }}</p>
                <input type="text" name="motif" id="motif">
            </div>
        </div>
        <input type="button" value="Retour" onclick="history.back()">
    {% endif %}
{% endblock %}